Git , github

what is git highly efficent version control local
what is github cloud hosting provider

why to use git
why to use github


whole project is working directory
commits are changes
repositories are where version control happens

repository vs working directory 

repository is broken into 2 things, staging area: starting file, commits: changes

merge helps you combine two of the working branches
commit
add
snapshots

how git works ?

branches: different parts of the project
master/main

essay / paper

first_doc.txt
first_doc_v1.txt
first_doc_v2.txt

cls

git --version : is to check your installed git version

git status : 

git init : initialize git repo 

git add : 

git add <filename>
git add . : currently working directory

git commit : commits all changes 

git commit -m 'this is your message' .

git config user.email 
git config user.name 

git log

git checkout commitid

git checkout master 

git branch

git branch name

git branch second-branch

master
    git checkout second-branch

second-branch
git checkout third-branch

git checkout -b third-branch

git merge : be in the branch where you want things to be copyed into
git merge destination branch

master 
    git merge third-branch

git rm filename

git add .
git commit -m '' .
git push : git push origin master
git push : git push origin main
asdfasdf

PAT: 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


asdfasdf